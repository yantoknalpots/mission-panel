# Mission Panel (v2) - Rewritten

This repo is the new OpenClaw Mission Panel (v2) scaffold. Implemented per new spec.

Structure:
- frontend/  -> Next.js minimal dashboard
- backend/   -> FastAPI minimal API

Dev:
- frontend: npm install && npm run dev
- backend: python -m uvicorn app.main:app --reload --port 8000
